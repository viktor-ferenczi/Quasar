# Quasar/wwwroot/viewer/index.html

Static shell for the Quasar grid viewer. It loads the viewer CSS and ES modules, provides controls for scene reload, local Space Engineers `Content` folder selection, a hidden folder file input fallback for browsers without `showDirectoryPicker`, render toggles including `Show lighting`, stats, and warning logs, and hosts the full-window Three.js viewport.
